# Local MailHog SMTP capture (see docker-compose.yml). Defaults match the
# docker-compose.yml service, so these are only needed to override.
MAILHOG_SMTP_ADDRESS=localhost
MAILHOG_SMTP_PORT=1025
AWS_REGION=
AWS_S3_BUCKET=
PGUSER=
PGPASSWORD=
PGHOST=
PGPORT=
PGDATABASE=