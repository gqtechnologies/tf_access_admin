# Frontend Guidelines

When building frontend UI, prioritize using shadcn components already installed in `components/ui`.

When a needed shadcn component is missing, install it with:

```sh
npm run shadcn -- add hover-card -y
```
