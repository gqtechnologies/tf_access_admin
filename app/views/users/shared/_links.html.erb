<%- if controller_name != 'passwords' %>
  <div class="flex justify-center my-2 w-full">
    <%= link_to t("frontend.admin.passwords.new.title"), new_password_path(resource_name), class: shadcn_button_classes(variant: :outline, size: :default, class_name: "w-full") %>
  </div>
<% end %>

<%- if controller_name != 'sessions' %>
  <div class="flex justify-center my-2 w-full">
    <%= link_to t("frontend.admin.login.title"), new_session_path(resource_name), class: shadcn_button_classes(variant: :outline, size: :default, class_name: "w-full") %>
  </div>
<% end %>